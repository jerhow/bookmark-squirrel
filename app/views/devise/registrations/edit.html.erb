<div style="width: 90%; margin-left: 10px;">

  <h2>User Settings</h2>

  <p><%= link_to "&larr; Back to user profile".html_safe, show_user_path %></p>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="user_edit_section">
      <div class="user_edit_field_1"><%= f.label :email %></div>
      <div class="user_edit_field_2">
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="user_edit_section">
      <div class="user_edit_field_1"><%= f.label :name %></div>
      <div class="user_edit_field_2"><%= f.text_field :name %></div>
    </div>

    <div class="user_edit_section">
      <div class="user_edit_field_1">
        <%= f.label :password %>
      </div>
      <div class="user_edit_field_2">
        <%= f.password_field :password, autocomplete: "new-password" %> 
          <br />
          <span class="user_edit_field_message">(leave blank if you don't want to change it)</span>
        <% if @minimum_password_length %>
          <!-- <br />
          <em><%= @minimum_password_length %> characters minimum</em> -->
        <% end %>
      </div>
    </div>

    <div class="user_edit_section">
      <div class="user_edit_field_1"><%= f.label :password_confirmation %></div>
      <div class="user_edit_field_2">
        <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
      </div>
    </div>

    <div class="user_edit_section" style="height: auto;">
      <div class="user_edit_field_1" style="height: auto;">Avatar</div>
      <div class="user_edit_field_2" style="height: auto;">
        <%= image_tag user_avatar(resource, 100) %>
        
        <div class="break" style="height: 5px;"></div>

        <%= f.file_field :avatar %>
        
        <div class="break" style="height: 12px;"></div>
        
        <%= link_to "Delete photo?", delete_user_avatar_path(resource), 
          method: :delete, data:{ confirm: 'Are you sure?' } %>
        
        <div class="break" style="height: 5px;"></div>

      </div>
    </div>

    <div class="break" style="height: 5px;"></div>

    <div class="user_edit_section">
      <div class="user_edit_field_1">
        <%= f.label :current_password %>
      </div>
      <div class="user_edit_field_2">
        <%= f.password_field :current_password, autocomplete: "current-password" %>
          <br />
          <span class="user_edit_field_message">(this is required to make changes to your profile)</span>
      </div>
    </div>

    <div class="actions">
      <%= f.submit "Update user settings", class: "submit_button", style: "margin-top: 15px;" %>
    </div>
  <% end %>

  <div class="break" style="height: 50px;"></div>

  <div class="user_edit_section" style="border-top: 2px dotted red;">
    <h3 style="color: red;">Cancel your account?</h3>
    <div class="user_edit_field_1">
      <%= button_to "Yes, cancel my account", registration_path(resource_name), 
        style: "color: red; font-size: .85rem;", 
        data: { confirm: "Are you sure?" }, method: :delete %>
    </div>
    <div class="user_edit_field_message" style="clear: both;">
      NOTE: Canceling your account is permanent. Your login and data cannot be restored.
    </div>
  </div>

  <div class="break" style="height: 100px;"></div>

</div>
